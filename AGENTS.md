# AGENTS.md

Neovim config built from scratch (migrated from lazy.nvim). Native features first; plugins secondary.

## Structure
`init.lua` requires `lua/config/{options,keymaps,diagnostics,autocmds}.lua`. No plugin manager configured yet.

## Commits
Conventional commits. Subject ≤50 chars, body wrapped at 72 chars.
